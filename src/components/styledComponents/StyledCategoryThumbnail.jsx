import styled from "styled-components";

const StyledCategoryThumbnail = styled.div`
  display: flex;
  flex-grow: 1;
  transition: all 0.07s linear;
  cursor: pointer;
`;

export default StyledCategoryThumbnail;