import { createTheme, ThemeOptions } from "@mui/material";

export const MUITheme: ThemeOptions = createTheme({
    palette: {
        primary: {
            main: "#000000"
        },
        secondary: {
            main: "#FFFFFF"
        }
    }
});
